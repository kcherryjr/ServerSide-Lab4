# ServerSide-Lab4
Lab 4 Using Web APIs

## Issues
Only main issue I faced initially was getting another player to show but realized it was because i needed to "break" back to name input.
Have to also make sure that the player's name is correctly spelled or it won't register. Also doesn't work for WNBA players also.

## Features
Used tons of indexes since I used integers and strings alike. for example headers.index would find the position of an item inside the Headers list. I also used the row function within the headers.index so for whatever player i chose, it would go find the row of information for that player inside of the specific index for said player inside the headers list.

Created functions such as get_player_id , show_player_info, show_headline_stats which all contain information on what each function will end returning upon calling out to the API.

Discovered the break feature which is really nice, essentially immediately stops a loop and returns to the beginning.

## Implementation
